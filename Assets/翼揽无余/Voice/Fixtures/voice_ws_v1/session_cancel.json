{
  "wire": "voice-ws-v1",
  "direction": "upstream",
  "event": "session.cancel",
  "doc": "Client -> server. Cancels the bound turn (barge-in). Optional 'feedback' defaults to '停止'. Server pauses audio, runs handle_barge_in, emits barge_in.accepted, then a voice.state for barge_in.<status>.",
  "schema": {
    "type": "session.cancel",
    "session_id": "string",
    "turn_id": "string",
    "audio_stream_id": "string",
    "feedback": "optional string, default '停止', non-empty, length <= 256"
  },
  "required_keys": ["type", "session_id", "turn_id", "audio_stream_id"],
  "optional_keys": ["feedback"],
  "example": {
    "type": "session.cancel",
    "session_id": "vr-test-session-001",
    "turn_id": "turn-0001",
    "audio_stream_id": "stream-audio-001",
    "feedback": "停止"
  }
}
